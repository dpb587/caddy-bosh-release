<%=

bpm = {
  'processes' => [
    {
      'name' => 'caddy',
      'executable' => '/var/vcap/jobs/caddy/bin/exec',
      'capabilities' => [
        'NET_BIND_SERVICE',
      ],
      'limits' => {
        'open_files' => 32768,
      },
      'persistent_disk' => true,
      'additional_volumes' => p('readonly_paths').map{ |path| { 'path' => path } },
    },
  ],
}

JSON.pretty_generate(bpm)

%>
